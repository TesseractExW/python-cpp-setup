#include <iostream>
#include <string>

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);
    std::string name;
    std::cin >> name;
    std::cout << "Hello " << name << "\n"; 
    return 0;
}